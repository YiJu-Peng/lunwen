# 论文批注修改记录

由于当前 PR 创建入口不支持二进制 DOCX 文件差异，本次提交不直接包含修改后的 DOCX 二进制文件，而是提供可复现的文本脚本 `tools/apply_lw_comment_revisions.py`。

运行下面命令可生成批注修改版文档：

```bash
python3 tools/apply_lw_comment_revisions.py
```

默认读取 `2425_41_10475_080902_6020222035_LW. (1).docx`，输出 `2425_41_10475_080902_6020222035_LW_批注修改版.docx`。

脚本会完成以下修改，并保留原文档中的 6 条批注：

- 将一级标题、二级标题、三级标题以及图表标题的段前距和段后距统一设置为 0。
- 在用户综合用例图前的说明文字中补充教师角色描述，并同步说明管理员角色职责，使图文角色一致。
- 保留原 PNG 图像格式以兼容 Word/WPS，并调整系统功能结构图显示尺寸，避免因 SVG 媒体导致打开时报通用输入/输出错误。
- 保留原 PNG 图像格式以兼容 Word/WPS，并调整系统详细设计流程图显示尺寸，改善页面阅读效果。
- 设置文档打开时更新字段，便于在 Word/WPS 中刷新目录页码。

> 说明：上一版脚本曾将两张图替换为 SVG。经复现，部分 Word/WPS 读取器会因此提示 “General input/output error”。当前版本不再写入 SVG，也不会改写图片关系到 `.svg`，生成后会校验包内不存在 SVG 媒体。


## PR 创建说明

如果右上角 `CREATE PR` 报 “Binary files are not supported” 或 “General input/output error”，请确认本次 PR 只包含这些文本文件改动：

- `.gitattributes`
- `2425_41_10475_080902_6020222035_LW_revision_notes.md`
- `tools/apply_lw_comment_revisions.py`

本分支已经把本地 `master` 固定在原始带批注文档提交 `67a71a7`，并把可复现修改放在 `thesis-comment-revisions` 分支上；这样 PR 差异不会包含重新生成后的 `.docx` 二进制文件。

如需指定输出路径，可执行：

```bash
python3 tools/apply_lw_comment_revisions.py --output /path/to/批注修改版.docx
```
